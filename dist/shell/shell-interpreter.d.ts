import type { ShellResult, ShellCommand } from "./shell-types";
import type { MemoryVolume } from "../memory-volume";
import type { ShellOptions } from "./shell-options";
export type SpawnChildCallback = (command: string, args: string[], opts?: {
    cwd?: string;
    env?: Record<string, string>;
    stdio?: "pipe" | "inherit";
}) => Promise<{
    pid: number;
    exitCode: number;
    stdout: string;
    stderr: string;
}>;
export declare class NodepodShell {
    private volume;
    private cwd;
    private env;
    private commands;
    private lastExit;
    private aliases;
    private history;
    private _execQueue;
    private _spawnChild;
    private shellOptions;
    private pipefail;
    private errexit;
    private nounset;
    private globEnabled;
    private nullglob;
    private dotglob;
    private positional;
    private shellName;
    private recursionDepth;
    private lastBackgroundPid;
    private pipeStatuses;
    private functions;
    private jobs;
    private detachedJobs;
    private nextJobId;
    private activeSignal;
    private backgroundStdout;
    private backgroundStderr;
    private commandSubstitutionDepth;
    private arithmeticDepth;
    private lastCommandSubstitutionExit;
    private processSubstitutionCounter;
    private processSubstitutionScopes;
    private directoryStack;
    private traps;
    private readonlyNames;
    private localScopes;
    private signalCleanup;
    constructor(volume: MemoryVolume, opts?: {
        cwd?: string;
        env?: Record<string, string>;
        shell?: ShellOptions;
    });
    registerCommand(cmd: ShellCommand): void;
    setSpawnChildCallback(cb: SpawnChildCallback | null): void;
    getSpawnChildCallback(): SpawnChildCallback | null;
    getCwd(): string;
    setCwd(cwd: string): void;
    getEnv(): Record<string, string>;
    setCancellationSignal(signal: AbortSignal | null): void;
    setBackgroundOutputCallbacks(onStdout: ((text: string) => void) | null, onStderr: ((text: string) => void) | null): void;
    exec(command: string, opts?: {
        cwd?: string;
        env?: Record<string, string>;
        signal?: AbortSignal;
    }): Promise<ShellResult>;
    private execWithDeadline;
    private _execInner;
    private execList;
    private execPipeline;
    private execCommand;
    private execBashList;
    private execBashAndOr;
    private execBashPipeline;
    private execStreamPipeline;
    private execBashCommand;
    private execBashCase;
    private execBashArithmetic;
    private evalArithmeticExpression;
    private evalArithmeticExpressionUnchecked;
    private execBashConditional;
    private execBashSelect;
    private execBashCoproc;
    private expandCaseText;
    private casePatternMatches;
    private execBashSimple;
    private expandBashWords;
    private createExpansionContext;
    private execSet;
    private execLet;
    private execReadonly;
    private execLocal;
    private execGetopts;
    private execShopt;
    private execTrap;
    private execScriptFile;
    private execJobsBuiltin;
    private createExecutionClone;
    private describeBashCommand;
    private assertCollectedOutput;
    private assertEnvironmentState;
    private canContinueLoop;
    private buildContext;
    private installVirtualOs;
    private resolvePath;
    private resolveFromPath;
    private normalizePath;
    private applyRedirects;
    private historyPath;
    private loadHistory;
    private persistHistory;
    private recordHistory;
    private trimHistory;
    private handleHistory;
    private expandCommandSubstitution;
    private handleAlias;
    private handleSource;
    private execNested;
}
